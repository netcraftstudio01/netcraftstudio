# NetCraft Studio - Environment Variables Template
# IMPORTANT: Copy this file to .env and fill in your actual credentials
# NEVER commit .env file to Git - it contains sensitive information!

# ==================================================
# FRONTEND CONFIGURATION
# ==================================================

# Vite API URL - Backend server address
# DEVELOPMENT: http://localhost:5000 (no trailing slash!)
# PRODUCTION: https://your-backend-url.com (no trailing slash!)
# IMPORTANT: Do NOT include trailing slash - URLs are constructed as ${VITE_API_URL}/api/...
VITE_API_URL=http://localhost:5000

# Port for Express server
PORT=5000

# ==================================================
# EMAIL CONFIGURATION (Gmail)
# ==================================================

# Gmail account email
EMAIL_USER=your-email@gmail.com

# Gmail App Password (generate from: https://myaccount.google.com/apppasswords)
# NOT your regular Gmail password!
EMAIL_PASSWORD=your-app-specific-password

# ==================================================
# DATABASE CONFIGURATION (Supabase PostgreSQL)
# ==================================================

# Supabase Database Host
DB_HOST=your-project.pooler.supabase.com

# Database Port (default: 5432)
DB_PORT=5432

# Database Name
DB_NAME=postgres

# Database User (format: postgres.xxxxxx)
DB_USER=postgres.xxxxxxxxxxxxx

# Database Password
DB_PASSWORD=your-supabase-database-password

# Supabase Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (for client-side operations)
SUPABASE_ANON_KEY=eyJhbGc...

# Supabase Service Role Key (for server-side operations - KEEP PRIVATE!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# ==================================================
# CLOUDINARY CONFIGURATION (Image Hosting)
# ==================================================

# Cloudinary Cloud Name
CLOUDINARY_CLOUD_NAME=your-cloud-name

# Cloudinary API Key
CLOUDINARY_API_KEY=your-api-key

# Cloudinary API Secret (KEEP PRIVATE!)
CLOUDINARY_API_SECRET=your-api-secret

# ==================================================
# RUNTIME ENVIRONMENT
# ==================================================

# Node environment: development, production, test
NODE_ENV=development

# ==================================================
# SETUP INSTRUCTIONS
# ==================================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy: cp .env.example .env
# 2. Fill in all credentials (see steps below)
# 3. Run: npm install
# 4. Run backend: npm start (on port 5000)
# 5. Run frontend: npm run dev (on port 5173)
#
# VERCEL DEPLOYMENT (Frontend):
# 1. In Vercel Dashboard, add Environment Variables:
#    - VITE_API_URL = https://your-backend-domain.com (NO trailing slash!)
#    - EMAIL_USER = your-email@gmail.com
#    - EMAIL_PASSWORD = your-app-password
#    - DB_* = database credentials
#    - CLOUDINARY_* = cloudinary credentials
#    - SUPABASE_* = supabase credentials
#
# 2. Redeploy after adding environment variables
#
# BACKEND DEPLOYMENT (Node.js server):
# Must deploy somewhere like Render, Railway, or your own server
# Set VITE_API_URL on frontend to point to your backend URL
#
# CORS CONFIGURATION:
# The server.js automatically allows:
#   - http://localhost:5173 (local dev)
#   - http://localhost:3000 (alternative port)
#   - https://www.netcraftstudios.org (your domain)
#   - https://netcraftstudio.vercel.app (Vercel domain)
# 
# To add more origins, edit server.js allowedOrigins array
#
# 1. Get Supabase Credentials:
#    - Go to https://app.supabase.com
#    - Create a new project or use existing
#    - Get credentials from Project Settings > Database
#
# 2. Setup Gmail:
#    - Enable 2-Step Verification on Gmail account
#    - Generate App Password: https://myaccount.google.com/apppasswords
#    - Use the 16-character password
#
# 3. Setup Cloudinary:
#    - Sign up at https://cloudinary.com
#    - Get cloud name, API key, and API secret
#    - Add to .env
#
# 4. Fill in the .env file with your actual values
#
# 5. IMPORTANT: Never commit .env to Git!
#    It should be in .gitignore (it is by default)
#    The .gitignore file ensures it won't be committed.
#
